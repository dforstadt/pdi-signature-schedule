[build]
  publish = "."

# Force redirect root to portal — overrides default index.html behaviour
[[redirects]]
  from = "/"
  to = "/portal.html"
  status = 301
  force = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Cache-Control = "no-store, no-cache, must-revalidate"
